name: flutter_sigma
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  font_awesome_flutter: ^10.0.0
  cupertino_icons: ^1.0.8
  firebase_core: ^3.5.0  # Versão estável atual
  firebase_auth: ^5.3.0   # Versão compatível
  cloud_firestore: ^5.4.2
  dio: ^5.7.0
  logging: ^1.0.2
  logger: ^2.4.0
  http: ^1.2.2
  provider: ^6.1.2
  intl: ^0.19.0
  mask_text_input_formatter: ^2.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
